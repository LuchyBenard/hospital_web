export const metadata = {
  title: "Find a Doctor",
  description:
    "Browse board-certified physicians at Providence General Hospital by specialty, language, availability, and patient rating.",
};

export default function DoctorsLayout({ children }) {
  return children;
}
